# Root-level requirements for local development, testing, and linting
# across this project. Install with:
#   pip install -r requirements.txt --break-system-packages
#
# Note: lambda/requirements.txt lists what's relevant to the Lambda
# function specifically (boto3 is already provided by the Lambda
# runtime, so it does not need to be packaged for deployment).

boto3>=1.34.0        # AWS SDK - used by the Lambda function and test/deploy scripts
pytest>=8.0.0        # Unit tests (lambda/test_threat_processor.py)
cfn-lint>=1.0.0      # Validates infrastructure/cloudformation/template.yaml
PyYAML>=6.0          # YAML parsing helper for local tooling
